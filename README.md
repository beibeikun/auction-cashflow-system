# 现金会协同系统

这是用于替代 `现金会v3.0.xlsx` 的本地协同 Web 应用。一台电脑启动服务，其他电脑在同一局域网用浏览器访问，即可共同录入拍卖成交、确认收付款、查看精算传票并导出完整明细。

## 启动

生产模式：

```bash
cd auction-app
npm install
npm run build
npm start
```

启动后终端会显示：

- `http://localhost:4173`：本机访问
- `http://局域网IP:4173`：其他电脑访问

开发模式需要开两个终端：

```bash
cd auction-app
npm start
```

```bash
cd auction-app
npm run dev
```

开发页面访问 `http://localhost:5173`，API 会代理到 `http://localhost:4173`。

## 技术栈

- Vue 3：前端界面和状态管理。
- Vite：开发服务器和生产构建。
- Node.js 原生 HTTP 服务：API、SSE 协同刷新、生产静态文件服务。
- JSON 文件存储：全局客户簿保存在 `data/store.json`，每个拍卖场次保存在 `data/sessions/<session-id>.json`。
- Python 标准库脚本：从 `.xlsx` 导入初始数据。

## 工作区

- `现场录入`：拍卖期间快速录入拍品编号、货主缩写、拍品缩写、数量、买家号牌和千单位成交价，也可选择指定拍品直接修改。
- `成交登记`：集中查看成交表，支持批量收款确认、付款确认、退货确认、清除确认和删除。
- `精算结算`：按客户或客户簿引用生成买方应收、卖方应付和最终请款/打款额。
- `客户资料`：维护本场客户登记和永久客户簿；可从客户簿引用老客户，也可新登记并自动加入客户簿。
- `场次设置`：新建、切换、修改、删除拍卖场次，并维护当前场次名称、默认佣金率、精算传票公司资料、拍品代码和出货号牌代码。
- `完整明细`：查看并导出与旧 Excel `完整信息` 类似的成交明细 CSV。

## 文档

- [完整开发文档](docs/完整开发文档.md)：面向开发维护，包含架构、数据模型、API、计算规则和发布维护说明。
- [详细操作说明](docs/详细操作说明.md)：面向日常操作人员，覆盖新场次准备、录入、结算、导出和归档。
- [新手快速使用说明](docs/新手快速使用说明.md)：面向第一次使用者，按最短流程完成常用操作。

## 从 Excel 导入

```bash
python3 scripts/import_workbook_data.py "../docs/现金会v3.0.xlsx"
```

也可以指定其他工作簿：

```bash
python3 scripts/import_workbook_data.py "/path/to/现金会v3.0_alpha_0325现金会-2.xlsx"
```

导入会覆盖当前拍卖场次对应的 `data/sessions/<session-id>.json`。如果还没有场次，脚本会先创建一个当前场次；共享永久客户簿仍保存在 `data/store.json`。

## 从 CSV 导入

在 `场次设置` 页面选择 CSV 文件后点击 `导入 CSV`。勾选 `只导入客户信息` 时只更新本场客户登记；不勾选时，系统会同时导入可识别的成交行。CSV 导入沿用旧格式，不会自动写入永久客户簿。

常用表头包括：

```text
客户号牌,出货号牌,客户名称,货主实际名称,客户电话,出货佣金比率,买货佣金比率,退货佣金比率
拍品编号,货主出货号牌缩写,拍品名称缩写,拍品数量,买家客户号牌,千单位成交价
```

`清空本场拍卖与客户数据` 会清空成交记录和本场客户登记，保留永久客户簿、代码和场次设置。

## 数据文件

数据拆分保存为：

```text
auction-app/data/store.json              # 全局配置、当前场次 ID、共享永久客户簿
auction-app/data/sessions/*.json         # 各拍卖场次的设置、代码、本场客户、成交记录
auction-app/data/deleted-sessions/*.json # 从界面删除后移入的场次文件
```

拍卖会当天建议定时复制 `data/store.json` 和 `data/sessions/` 做备份。服务端每次新增、修改、删除都会自动写入对应 JSON，并通过实时事件通知其他已打开页面刷新。

## 录入规则

- 成交价仍按旧表方式录入千单位，例如 `8` 表示 `8,000`。
- 买家号牌填 `-1` 表示流拍。
- 买家确认、卖家确认、退货确认通过成交行里的 `收`、`付`、`退` 按钮切换。
- 系统会自动计算成交金额、买卖佣金、消费税、结算额、退回判定和结算状态。
